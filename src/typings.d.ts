// typings.d.ts
declare global {
    interface Window {
      ethereum: any; // Declaring the ethereum property on the window object
    }
  }
  
  export {}; // Ensure the file is treated as a module
  